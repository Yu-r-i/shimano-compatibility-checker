# dependencies
node_modules/

# build outputs
dist/

# caches
.cache/
node_modules/.cache/

# Cloudflare / Wrangler
.wrangler/

# env (VITE_* defaults are committed via .env / .env.example; local overrides are ignored)
.env.local
.env.*.local
